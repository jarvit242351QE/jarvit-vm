{
  "$schema": "https://openclaw.ai/schemas/plugin.json",
  "id": "vm-updater",
  "name": "vm-updater",
  "version": "2.0.0",
  "description": "Handles VM software updates with AI-powered merge. Runs standalone server on port 18790 to avoid SPA fallback.",

  "configSchema": {
    "type": "object",
    "properties": {
      "manifestPath": {
        "type": "string",
        "default": "/opt/jarvit/vm-manifest.json",
        "description": "Path to the current manifest file"
      },
      "protectedPaths": {
        "type": "array",
        "items": { "type": "string" },
        "default": [
          "/opt/jarvit/config/jarvit.json",
          "/data/config/jarvit.json",
          "/data/.jarvit/jarvit.json"
        ],
        "description": "Paths that are never overwritten during updates"
      }
    }
  }
}
