node_modules/
*.tgz
release-pkg/
/manifest.json
/jarvit-vm.tar.gz

# TypeScript build artifacts (source maps and declarations)
plugins/*/dist/*.d.ts
plugins/*/dist/*.d.ts.map
plugins/*/dist/*.js.map

# Keep compiled JS in dist/ (needed for plugin distribution)
# dist/*.js is explicitly tracked for vm-updater
